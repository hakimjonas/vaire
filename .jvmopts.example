# Local developer JVM tuning for sbt. Copy to `.jvmopts` (gitignored) to override.
# CI does NOT use this file — the Forgejo workflows set JAVA_OPTS/SBT_OPTS explicitly.

-Xms2G
-Xmx8G
-Xss4M
-XX:+UseZGC
