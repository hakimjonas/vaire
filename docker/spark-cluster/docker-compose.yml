services:
  spark-master:
    build: .
    image: strongbow-spark-cluster
    container_name: spark-master
    network_mode: host
    command: /opt/spark/bin/spark-class org.apache.spark.deploy.master.Master
    environment:
      SPARK_MASTER_HOST: 127.0.0.1
      SPARK_MASTER_PORT: "7077"
      SPARK_MASTER_WEBUI_PORT: "8080"
    volumes:
      - ../../strongbow-core/target/scala-3.8.2/classes:/opt/strongbow/core-classes:ro
      - ../../strongbow-spark/target/scala-3.8.2/classes:/opt/strongbow/spark-classes:ro

  spark-worker-1:
    image: strongbow-spark-cluster
    container_name: spark-worker-1
    network_mode: host
    depends_on:
      - spark-master
    command: /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker spark://127.0.0.1:7077
    environment:
      SPARK_WORKER_CORES: "4"
      SPARK_WORKER_MEMORY: 32g
      SPARK_WORKER_WEBUI_PORT: "8081"
    volumes:
      - ../../strongbow-core/target/scala-3.8.2/classes:/opt/strongbow/core-classes:ro
      - ../../strongbow-spark/target/scala-3.8.2/classes:/opt/strongbow/spark-classes:ro
      - /tmp:/tmp

  spark-worker-2:
    image: strongbow-spark-cluster
    container_name: spark-worker-2
    network_mode: host
    depends_on:
      - spark-master
    command: /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker spark://127.0.0.1:7077
    environment:
      SPARK_WORKER_CORES: "4"
      SPARK_WORKER_MEMORY: 32g
      SPARK_WORKER_WEBUI_PORT: "8082"
    volumes:
      - ../../strongbow-core/target/scala-3.8.2/classes:/opt/strongbow/core-classes:ro
      - ../../strongbow-spark/target/scala-3.8.2/classes:/opt/strongbow/spark-classes:ro
      - /tmp:/tmp
